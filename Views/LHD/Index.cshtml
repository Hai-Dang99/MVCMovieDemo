@model IEnumerable<MVCMovie.Models.LHD>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LHDName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LHDName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LHDID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LHDID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LHDID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
